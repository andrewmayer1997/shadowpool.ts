export type Share = {
  sealhash: string;
  isBlock?: boolean;
};

export const getShareByID = function () {
  //..
};

export const addShare = function (data: Share) {
  //..
};

export const getTotal = function (): [number, number] {
  return [0, 0];
};
